packages:
  - "apps/*"
  - "packages/*"

# Dependencies allowed to run install scripts. pnpm 11 blocks postinstall by
# default; if a newly added dependency mysteriously doesn't work, check here first.
allowBuilds:
  esbuild: true
  '@google/genai': true
  protobufjs: true
